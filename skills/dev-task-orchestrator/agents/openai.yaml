interface:
  display_name: "Dev Task Orchestrator"
  short_description: "Build reviewed PRs in isolated Git worktrees"
  default_prompt: "Use $dev-task-orchestrator to check the environment and turn this task into a reviewed pull request from an isolated worktree."
policy:
  allow_implicit_invocation: false
